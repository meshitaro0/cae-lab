# Phase 6 採点とフィードバック — 2026-09-26

対象はコミット `b3f8502` の [提出Notebook](../../notebooks/Phase6.ipynb) と [学習ログ](../learning-log.md)。提出時の設問・配点で採点し、今回追記した教材上の補足は遡って必須条件にしない。

**評価：84 / 100。** 配布データの数値計算、方向別収支とエネルギー収支の区別、出力サンプリングの実験は正確。主な修正点は、一般入力に対する初期値の扱い、自作検証が本当に目的の性質を検査しているか、観測した結果から言える結論の範囲である。

| 演習 | 小問別 | 得点 | 主な評価 |
| --- | --- | --- | --- |
| 1 | 5 + 4 + 5 | 14 / 15 | 実装・数値・自作入力は正しい。Misesを座標変換で一軸化した応力とする説明を修正 |
| 2 | 10 + 8 + 3 + 4 | 25 / 30 | 初期エネルギー差し引き、検証の比較先、入力保存の確認方法を修正 |
| 3 | 8 + 4 + 4 | 16 / 20 | 比較表と判定は正しい。細分化の効果と局所特異性に関する結論を修正 |
| 4 | 5 + 5 + 5 + 5 | 20 / 20 | 計算・図・解釈・設定感度の判定を満たす |
| 5 | 4 + 2 + 3 | 9 / 15 | 確認項目と仮説の選択は適切。現時点の採否と計画の判定範囲を具体化 |

## 1. 平均処理とMises応力

全9成分の縮約、静水圧、純せん断の実装・検証は正しい。各点120 MPa、平均応力のMisesは0 MPa、Misesの平均120 MPaという結果と相殺の説明も正しい。材料境界の平均を降伏判定に直結できないという制約も挙げられている。

小問2の「応力テンソルを一軸引張応力とみなせる座標系に変換したときの応力」は誤りのため1点減点。座標回転は主応力を変えないので、一般の三軸応力を一軸応力にはできない。例えば純せん断の主応力は $(\tau,0,-\tau)$、一軸引張は $(q,0,0)$ であり、回転では一致しない。

ここでの相当とは、**J2型の降伏判定に用いる偏差応力の大きさを、一軸引張の応力と同じ尺度で表す**という意味。平均化についての後半の説明はそのまま活かせる。

## 2. 力積とエネルギーの収支

### 小問1：10 / 12

力積の台形積分、初期運動量、期間による正規化、絶対値を取った最大残差は正しい。一方、エネルギーでは指定された初期値の差し引きが抜けている（2点減点）。必要な分子は

$$
[K_k-K_0]+[U_k-U_0]+D_k+H_k-W_k
$$

である。配布データでは $K_0=U_0=0$ のため差が見えなかった。基準台帳の全時刻のKへ47 J、Uへ29 Jを加えると、初期値との差は変わらず正しい残差は0だが、提出関数は $(47+29)/2000=0.038$ を返す。K,Uの初期値を差し引くことは提出時の表と本文で指定済みであり、今回追加した要件ではない。

なお、`eta_K` は指定どおりKそのものの最大値を使うため、Kの初期値を加えると変わってよい。エネルギー残差と比率で扱いを混同しない。

### 小問2：8 / 8、小問4：4 / 4

3ケースの表・合否は正しい。エネルギーの転記不備で0.08、力の転記不備で0.05となる結果も再現した。力の欠落と散逸の二重計上を別々に点検する説明は適切。

### 小問3：3 / 6

非等間隔時刻列と非ゼロのa,bを設計し、台形則で線形外力を正確に積分できることを確認できている。基準値2倍の実験も用意されているが、次の二点は検証として機能していない。

- **比較先（1.5点減点）**：`r_P` の比較に `out_nu_2er` を使っており、`force_ref`を変えた `out_nu_2fr` を検査していない。元の残差が丸め誤差程度なので、`np.allclose` の既定 `atol=1e-8` によって誤った比較でも通過する。`force_export_error`で確認すると、元が0.05、energy_refだけ2倍でも0.05、force_refだけ2倍では0.025となり、違いが明確になる。
- **入力保存（1.5点減点）**：`history_nu.copy()` は辞書だけの浅いコピーで、中の配列を共有する。関数が配列を破壊しても比較用の配列も変わり、検出できない。例えば `{k: v.copy() for k, v in history_nu.items()}` で独立に保存して比較する。提出関数そのものは別途の検査で入力を変更しないと確認できたため、ここでの減点は検証方法に限定する。

**許容値についての質問への回答**：基準値を2倍にした結果の一致確認は、例えば $\max|r_{\rm original}-2r_{\rm doubled}|\le10^{-12}$ とし、`np.allclose(..., rtol=0, atol=1e-12)` と書けばよい。これは無次元の計算一致を調べる許容値。収支の実務的な合否に使う1%とは目的が違う。

提出時にこの一致許容値を指定しなかったのは出題側の不足であり、`rtol=0.01` の選択自体は減点しない。非ゼロ残差の利用も今回の補足で、使わなかったこと自体に別の減点はしない。

## 3. メッシュ感度

小問1は8 / 8。最後の2メッシュの相対差は、反力約0.4525%、遠方応力約0.5128%、角部Mises 27.5%、EPS 40%で、指定1%基準の判定は正しい。

小問2は4 / 6。絶対差・相対差の傾向を読めており、積分精度など他の誤差への着眼もよい。ただし「メッシュ細分化により改善するのは形状再現性」という説明に限定した点で2点減点。形状が既に正確でも、細分化は変位・ひずみ・応力場を近似する自由度と空間解像度を増やす。Phase 4の直線棒がその例である。今回の差は二つの近似解の差であり、厳密解との距離そのものではない。1%を満たしたことから厳密誤差も1%以下とは言えない。

小問3は4 / 6。反力と局所破断の用途を分け、軟化則とひずみ集中を原因候補にしたことを評価する。二つの候補が関連していること自体は減点しない。

- 「反力を評価するうえでは特異性は表れていない」は、**反力という関心量への影響が小さい**という意図なら理解できるが、この表から局所の特異性の有無は判断できない（1点減点）。「今回の細分化範囲で反力の変化は暫定基準内。局所特異性の有無は別途確認」と書くと範囲が明確になる。
- 破壊エネルギーが要素寸法に応じて変わると「破断までの塑性ひずみが大きくなる」という方向は、変化の仕方や軟化則が未指定なので一般には決まらない（1点減点）。「破断ひずみや局所化幅が要素寸法に依存し得るため、そのモデルと正則化を確認する」までなら根拠に合う。

## 4. サンプリングと設定感度

20 / 20。最大値116.484 Nと1000 N、粗い出力での過小評価約88.35%、補間しても最大値が変わらないことを正しく示している。負荷時間に対する約8.11%、追加質量に対する約2.78%という反力感度と2%基準の不合格も正しい。

小問4は、8%の反力感度を理由に採用できないことを述べ、慣性の影響を調べる変形モードも挙げているため満点とする。さらに実施時には、関心部品のどこを比較するかを指定すると使いやすい。これは追加の改善案で、今回の減点対象ではない。

厳密にはAはソルバを実行していない信号実験なので、「ソルバ時間刻みが変わっていない」より「ソルバ時間刻みを検証する実験ではない」が正確。区別の理解は認め、言い換えだけの再提出は不要。

「出力間隔を変えているのに何を検証したかと聞かれて混乱した」という指摘を受け、問題文を、補間の限界を説明し、そこからソルバ時間積分精度を評価できない理由を述べる形へ明確化した。新しい証明や実験は要求しない。

## 5. 未確認ケースのレビュー計画

### 小問1：4 / 4

EPSの定義、最大応力の位置、負荷時間、出力間隔という4項目は、それぞれ判断目的につながっている。設問は各観点一つなので、全項目を網羅していないことでは減点しない。

### 小問2：2 / 4

角部破断を保留した判断は正しい。反力比較を「変形モードが同一なら支持」とした点で2点減点。現時点ではその同一性も確認されておらず、同じ形に変形しても材料剛性・境界条件・慣性・離散化による反力の偏りは残り得る。設計案間で誤差が打ち消し合う保証もない。現段階では反力比較も保留し、**どの感度がどの範囲なら相対比較へ進めるか**を条件として残す。

### 小問3：3 / 7

出力間隔・実験・負荷時間という三つの仮説と優先順、観測量、判断基準を用意したことに3点。提案そのものは活かせる。修正が必要な4点は次のとおり。

1. **出力間隔の比較からの結論（1点減点）**：最大Misesの変化が10%未満でも、「最も危険な時刻を出力できた」とは保証できない。両方が狭いピークを逃す可能性があり、危険度もMisesだけでは決まらない。「試した出力間隔の範囲で、保存されたMises最大値の感度が暫定基準内」と限定する。10%という値そのものは、自分で選んで理由を付ける課題なので減点しない。
2. **独立実験との比較の定義（1点減点）**：「実測ひずみ」と「累積相当塑性ひずみ」は自動的に同じ量にならない。測れるひずみの定義を揃えるか、破断の有無・開始変位など観測可能な量で、事前固定した判定を比較する。95%正答率を採用するなら何を1件と数えるか、材料同定・閾値調整用とは別の試験かも書く。特定の試験数や統計手法までは要求しない。
3. **実験の未確認事項（1点減点）**：取得可能性と測定不確かさが未確認の場合の条件付けがない。これは提出時の設問で要求していた。例えば「独立試験を取得でき、開始変位の測定幅が判定幅に対して十分小さい場合に評価する」と記す。
4. **負荷時間の比較からの結論（1点減点）**：変形モード一致だけで「準静的解析の前提を壊していない」までは言えない。形が同じでも反力は変化し得る。結論を変形モードに限定するか、同一押込み変位の反力差など、用途の判断に必要な量も検査する。演習4でこの反力感度は正しく評価できているので、その考えを未確認ケースにも適用すればよい。

固定条件欄は代表項目が書かれていると解釈し、他条件も変更する意図とは扱わない。改訂では「変更・取得欄以外は固定」と宣言し、実験なら材料だけでなく形状・拘束・負荷条件も比較可能か確認すると、計画が読みやすくなる。

## 確認事項への返答と教材側の反映

1. **既知条件と一般レビューの区別**：改善が有効だったと記録。演習5でも未確認情報を挙げる部分はできている。
2. **三つの残差**：説明の方向は合っている。補足すると、降伏面からの距離をゼロにすべきなのは塑性活動中であり、弾性時の負の降伏関数は正常。降伏条件だけでは流れ則や履歴更新全体の正しさまでは保証しない。またエネルギー収支は、与えたモデル内での仕事・蓄積・散逸の整合性を検査する。現実を表すかという意味の妥当性確認には独立実験等が必要。この会話上の補足は演習の点数から追加で減点しない。
3. **関心量・用途・計画の接続**：演習3・4の数値感度は読めている。演習5では確認する仮説も適切だが、観測した証拠より結論を広く取る傾向が残った。上記の範囲限定が次の確認点。
4. **負担のバランス**：適切だったと記録。長い手計算を増やさず、短い修正で確認する。

ピーク付近だけ出力を細かくする着眼は適切。イベント時刻が条件変更でずれる可能性を見込み、細かい区間に余裕を持たせるとよい。演習5の自由度には、唯一の模範計画に合わせるのではなく、仮説・証拠・結論の対応を評価することを問題文へ補足した。

本文にはMisesと座標回転の区別、各残差の意味の限界、場の近似とメッシュ細分化を補足した。Notebookは設問2・4・5への説明追記のみで、解答・コード・出力は保持。PROFILEは既存方針の有効性確認として更新せず、ROADMAPと学習ログを採点済み・修正確認待ちへ更新した。

## 実行確認と次の作業

- UTF-8 JSON読込、全5コードセルのcompile・順次実行、配布チェックと提出されたassertの成功を確認。図は非対話描画環境で描画処理まで実行し、GUI表示は行っていない。
- 主値差によるMisesの別計算、非ゼロ初期エネルギー、非ゼロ残差での基準値変更、独立コピーによる入力不変を確認した。
- 実行確認前後の提出NotebookのSHA256は `4a1a6c99a64975e219fb8894a193cc00bcdabac9654f4c2c1a7967430af20d54` で一致。その後の設問への追記は別の教材改訂である。

次回は、演習1のqの説明、演習2の初期値と二つの検証方法、演習3の二つの解釈、演習5の採否・計画を確認する。計算表や演習4の再提出、新たなFEM実装は不要。現時点ではPhase 6の修正確認待ちとし、完了判定はその確認後に行う。
